[0.1.28]
* Fixed a bug that truncated the list of provider announcements
* Added npub names to the TUI

[0.1.27]
* Proactive topup now maintains the API-key balance margin ahead of requests, allowing more parallel requests
* Updated bundled routstrd to 0.4.9 and routstr-sdk to 0.4.4
* Removed the cocod dependency; the container now generates its Nostr identity on first boot
* Added npub names, manual/scheduled client refresh, and payment confirmation for receive to the CLI
* Fixed a wallet-lock race when restarting the daemon with in-flight requests, and paid mint quotes now settle immediately

[0.1.26]
* Model allowlist enforcement is now disabled by default; set ROUTSTRD_AUTH_MODEL_ALLOWLIST=true to enable it
* Added a nullable name field to npubs (POST/PATCH/GET), with automatic DB migration for existing installs

[0.1.25]
* Added a models allowlist to only allow whitelisted models through the proxy
* Security: removed the hardcoded default admin npub from the Docker entrypoint; deployments must now bootstrap an admin explicitly
* Security: env-sourced admin npubs removed from the env var are deleted at startup instead of persisting

[0.1.24]
* Added CORS support for browser clients
* Fixed public path handling to only treat reads as public
* Fixed API key exposure in daemon control endpoints

[0.1.23]
* Added TEE E2EE support to routstr-sdk and routstrd
* Major fixes and stability improvements

[0.1.22]
* Fix for streams being cut off in the middle, faster TUI rendering, LLM cache tokens display in the recent usage tab, and more fixes

[0.1.21]
* Added functionality to remove stale receive operations to cocod

[0.1.20]
* Added NWC for Routstrd

[0.1.19]
* Fixed routstr SDK cache issue

[0.1.18]
* Added logging
* Fixed the 0 balance API key refund bug
* Fixed debug logging on Docker
* Added npub usage view for admins
* Added client IDs for recent usage

[0.1.17]
* Added support for updating the role of an npub

[0.1.16]
* Remove stale cocod PID/socket files from older container runs when running under vanilla Docker

[0.1.15]
* Moved the client id npub suffix logic to routstrd auth
* Added filters for clients id lists and usage stats to particular npubs

[0.1.14]
* Reduced node cooldown period from 5 minutes to 42 seconds

[0.1.13]
* Fixing cloudron cache issue

[0.1.12]
* Fixing cloudron cache issue

[0.1.6]
* asdf

[0.1.5]
* Remove client identity header injection on upstream forwarding
* Conditional auth header stripping: Bearer/sauth passed through, NIP-98 stripped

[0.1.4]
* Fixed cocod dir in routstrd

[0.1.3]
* Cocod bug fix

[0.1.2]
* Fixed routstrd dir env variables
* Added npub bootstrapping to auth

[0.1.1]
* Bump version

[0.1.0]
* Initial release
